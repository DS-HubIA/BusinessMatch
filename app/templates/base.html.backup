<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Business Match - SEBRAE{% endblock %}</title>
    <style>
        :root {
            --primary: #2563eb;
            --secondary: #6b7280;
            --accent: #dc2626;
            --success: #16a34a;
            --light: #f8fafc;
            --dark: #1e293b;
            --muted: #64748b;
            --border: #e2e8f0;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #ffffff;
            color: #000000;
            line-height: 1.4;
        }
        
        .app-header {
            background: #2563eb;
            color: white;
            padding: 15px 20px;
            text-align: center;
        }
        
        .app-header h1 {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 2px;
        }
        
        .app-header .subtitle {
            font-size: 14px;
            font-weight: normal;
        }
        
        .bottom-nav {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            background: white;
            display: flex;
            justify-content: space-around;
            padding: 12px 0;
            border-top: 2px solid #e5e7eb;
            z-index: 1000;
        }
        
        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-decoration: none;
            color: #6b7280;
            font-size: 12px;
            font-weight: bold;
        }
        
        .nav-item.active {
            color: #2563eb;
        }
        
        .content {
            padding-bottom: 70px;
            min-height: 100vh;
        }
        
        .btn {
            background: #2563eb;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 6px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            width: 100%;
        }
        
        .btn-danger {
            background: #dc2626;
        }
        
        .btn-outline {
            background: white;
            color: #2563eb;
            border: 2px solid #2563eb;
        }
        
        .card {
            background: white;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 20px;
            margin: 16px;
        }
        
        .section-title {
            font-size: 18px;
            font-weight: bold;
            color: #000000;
            margin-bottom: 16px;
        }
        
        .form-group {
            margin-bottom: 16px;
        }
        
        .form-group label {
            display: block;
            font-weight: bold;
            margin-bottom: 8px;
            color: #000000;
        }
        
        .form-control {
            width: 100%;
            padding: 12px;
            border: 2px solid #e5e7eb;
            border-radius: 6px;
            font-size: 16px;
        }
        
        .danger-zone {
            border: 2px solid #dc2626;
            background: #fef2f2;
        }
        
        .danger-title {
            color: #dc2626;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="app-header">
        <h1>Rede SEBRAE de Negócios</h1>
        <div class="subtitle">Business Match FE 2025</div>
    </div>

    <div class="content">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                <div style="padding: 16px;">
                    {% for category, message in messages %}
                        <div class="card" style="background: {% if category == 'success' %}#f0fdf4{% else %}#fef2f2{% endif %}; border-color: {% if category == 'success' %}#bbf7d0{% else %}#fecaca{% endif %};">
                            {{ message }}
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
        {% endwith %}
        
        {% block content %}{% endblock %}
    </div>

    <!-- Bottom Navigation - Ícones do MVP -->
    <nav class="bottom-nav">
        <a href="{{ url_for('main.opportunities') }}" class="nav-item {% if request.endpoint == 'main.opportunities' %}active{% endif %}">
            <span style="font-size: 20px; margin-bottom: 4px;">💼</span>
            <span>Oportunidades</span>
        </a>
        <a href="{{ url_for('business.matches') }}" class="nav-item {% if request.endpoint == 'business.matches' %}active{% endif %}">
            <span style="font-size: 20px; margin-bottom: 4px;">🤝</span>
            <span>Matches</span>
        </a>
        <a href="{{ url_for('business.interests') }}" class="nav-item {% if request.endpoint == 'business.interests' %}active{% endif %}">
            <span style="font-size: 20px; margin-bottom: 4px;">👁️</span>
            <span>Interesses</span>
        </a>
        <a href="{{ url_for('main.profile') }}" class="nav-item {% if request.endpoint == 'main.profile' %}active{% endif %}">
            <span style="font-size: 20px; margin-bottom: 4px;">👤</span>
            <span>Perfil</span>
        </a>
    </nav>
</body>
</html>
